import { PointCloudDirective } from './point-cloud.directive';

describe('PointCloudDirective', () => {
  it('should create an instance', () => {
    const directive = new PointCloudDirective();
    expect(directive).toBeTruthy();
  });
});
